services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "${HOST_PORT:-3000}:3000"
    volumes:
      - paloma_mensajera_data:/app/prisma/data
    restart: unless-stopped

volumes:
  paloma_mensajera_data: